apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-plane-web.fullname" . }}-test-connection"
  labels:
    {{- include "data-plane-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "busybox:1.28"
      command: ['wget']
      args: ['{{ include "data-plane-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
