apiVersion: v1
kind: Service
metadata:
  name: data-plane-web
  labels: { { - include "data-plane-web.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "data-plane-web.selectorLabels" . | nindent 4 } }
