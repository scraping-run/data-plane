apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-plane-server.fullname" . }}-test-connection"
  labels:
    {{- include "data-plane-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "busybox:1.28"
      command: ['wget']
      args: ['{{ include "data-plane-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
